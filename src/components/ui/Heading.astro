---
interface Props {
    text?: string;
    textGradient?: string;
    level?: 1 | 2 | 3 | 4;
    className?: string;
}

const { text = "", textGradient = "", level = 1, className = "" } = Astro.props;
const Tag = `h${level}`;
---

<Tag class:list={[
    "leading-tight tracking-tight text-cream-900",
    level === 1 && "text-6xl md:text-7xl font-black",
    level === 2 && "text-4xl md:text-5xl font-black",
    level === 3 && "text-2xl md:text-3xl font-black",
    level === 4 && "text-xl font-black",
    className
]}>
    {text && (
        <>
            {text}
            {textGradient && " "}
        </>
    )}
    {textGradient && (
        <span class="bg-linear-to-r from-primary-400 to-primary-500 bg-clip-text text-transparent">
            {textGradient}
        </span>
    )}
</Tag>